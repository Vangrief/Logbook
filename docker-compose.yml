services:
  flightarchive:
    build: .
    # Pre-built image alternative (uncomment if pushing to a registry):
    # image: flightarchive:latest
    container_name: flightarchive
    restart: unless-stopped
    ports:
      # Host 7323 -> container 7323. Reachable at http://10.0.10.90:7323
      - "7323:7323"
    environment:
      - DATABASE_PATH=/data/flightarchive.db
      # REQUIRED: shared access password. The app refuses to start without it.
      - LOGBOOK_PASSWORD=${LOGBOOK_PASSWORD:?Set LOGBOOK_PASSWORD in .env}
      # Optional: secret used to sign session cookies. If omitted a random one
      # is generated on startup (sessions drop on container restart).
      - SESSION_SECRET=${SESSION_SECRET:-}
      # Optional bootstrap of OpenSky credentials via .env (otherwise set them
      # in the Settings page). These are NOT required at build time.
      - TZ=${TZ:-Europe/Zurich}
      - OPENSKY_CLIENT_ID=${OPENSKY_CLIENT_ID:-}
      - OPENSKY_CLIENT_SECRET=${OPENSKY_CLIENT_SECRET:-}
    volumes:
      # Unraid appdata share holds the SQLite database persistently.
      - /mnt/user/appdata/flightarchive:/data
